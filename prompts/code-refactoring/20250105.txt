Request
จาก function validate_password ที่สร้างด้วยภาษา Python เพื่อตรวจสอบรูปแบบของ password ให้เป็นไปตามที่กำหนด คือ ต้องมีความยาวอย่างน้อย 8 ตัว มีตัวเลขและอักขระพิเศษอย่างน้อย 1 ตัว
จง refactor function นี้เพื่อให้ อ่านได้ง่าย ทำงานได้มีประสิทธิภาพ  และมีเอกสารอธิบายใน source code
รูปแบบของผลลัพธ์ที่ต้องการจากคือมีส่วนของ introduction, วิเคราะห์, โค้ดที่ปรับปรุง

def validate_password(password: str) -> None:
    if password is None:
        raise ValueError("Password cannot be None.")

    if len(password) < 8:
        raise ValueError("Password must be at least 8 characters long.")

    special_chars = set(string.punctuation)

    if not any(char.isdigit() for char in password):
        raise ValueError("Password must contain at least one digit.")

    if not any(char in special_chars for char in password):
        raise ValueError(f'Password must contain at least one special character from the list {string.punctuation}.')
